{
  "name": "hvi-commitlint",
  "private": true,
  "description": "Pinned commitlint tooling for the commit-message CI check",
  "devDependencies": {
    "@commitlint/cli": "21.2.1",
    "@commitlint/config-conventional": "21.2.0"
  }
}
